header.header
    .container-fluid 
        .row.align-items-center
            .col-3 
                .inner-logo 
                    a(href="/")
                        h1 ADMIN

            .col-9 
                .inner-menu 
                    ul(class="d-flex justify-content-end gap-4 align-items-center") 
                        li 
                            a(href="/admin/dashboard") #{user.fullName} - #{role.title}
                        li 
                            a(
                                href=`${prefixAdmin}/auth/logout`
                                class="btn btn-danger"
                            ) Đăng xuất   