mixin formatDateTime(time)
    p #{moment(time).format("DD/MM/YYYY HH:mm:ss")}

mixin formatDate(time)
    p #{moment(time).format("DD/MM/YYYY")}

mixin formatTime(time)
    p #{moment(time).format("HH:mm:ss")}