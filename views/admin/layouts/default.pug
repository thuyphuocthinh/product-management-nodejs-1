doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/style.css")
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", )
        link(rel="stylesheet", href="/css/style.css")
        link(rel="stylesheet", href="/css/adminStyle.css")
        title= pageTitle
    body 
    
        if (role.permissions.length > 0)
            include ../../admin/partials/header.pug

            div.mainContent
                include ../../admin/partials/sider.pug
                block main 

            script(src="/js/admin/script.js") 
            script(src="/js/admin/products.js") 
            script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js")
            script(type='application/javascript' src='/tinymce/tinymce.min.js')
            script(type='application/javascript' src='/js/admin/my-tinymce-config.js')
        else 
            h1 You are not allowed to access this system